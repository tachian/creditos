[project]
name = "creditos-contracts"
version = "0.1.0"
description = "Contratos versionados públicos, internos e assíncronos do CreditOS"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["setuptools>=80"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
